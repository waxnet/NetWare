using UnityEngine;

namespace NetWare.Constants;

public static class WindowConstants
{
    public const int Width = 550;
    public const int Height = 650;

    public const int PositionX = 200;
    public static int PositionY = (Screen.height / 2) - (Height / 2);
}
